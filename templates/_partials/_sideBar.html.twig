<nav class="navbar navbar-expand-md navbar-dark bg-secondary fixed-left h-100 w-25">
	<div class="sidebar">
		<a href="/">Home</a>
		{% for cat in activeCategories %}
			<a href="{{ path('homePage', {cat: cat.id})}}" style="background-color: {{ cat.color}};">{{cat.name}}</a>
		{% endfor %}
	</div>
</nav>
